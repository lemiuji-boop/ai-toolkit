# MATFES Backend

FastAPI backend for the Multi-Agent Task Feasibility and Estimation System.

```bash
uv sync --extra dev
uv run pytest
uv run uvicorn app.main:app --reload --port 8000
```

See the [project README](../README.md) for architecture, frontend setup, and Docker usage.

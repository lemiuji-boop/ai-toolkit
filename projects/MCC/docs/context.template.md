# context — шаблон рабочего контекста итерации

Заполняется перед каждой сессией в Cursor (по CURSOR_РАБОТА.md §5). Держит агента в рамках одного FR.

## Текущая задача
- FR: {FR-ID}
- Сервис/модуль: {файл(ы)}

## Что трогать
- {список файлов, которые можно менять}

## Что НЕ трогать
- Публичный API: `app/core/schemas.py`, роуты `app/api/*`
- Соседние модули вне задачи

## Ссылки
- ТЗ: `docs/TZ.md` (раздел FR)
- Правила: `.cursor/rules/*.mdc`
- Трассировка: `docs/trace.md`

## Definition of Done итерации
- [ ] реализован только заявленный FR
- [ ] добавлен тест (без GPU/LLM), `pytest -q` зелёный
- [ ] `ruff` и `mypy` без ошибок
- [ ] строка в `docs/trace.md` обновлена
- [ ] публичный API не изменён (или изменён вместе со схемами и тестами)
